One-sentence: The solid brand-orange primary action button used for "Buscar" and other main calls to action.

```jsx
<PrimaryButton onClick={search}>Buscar</PrimaryButton>
<PrimaryButton block>Salvar seleção</PrimaryButton>
```

`block` makes it full-width. Presses scale slightly; disabled drops to 50% opacity.
