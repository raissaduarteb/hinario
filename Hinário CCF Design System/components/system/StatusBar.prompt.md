One-sentence: The iOS-style status bar (clock, signal, wi-fi, battery) that tops each phone screen — the one device-chrome component kept from the source mockup kit.

```jsx
<StatusBar tone="dark" />
<StatusBar tone="light" time="10:24" />
```

Use `tone="dark"` on light screens and `tone="light"` on the dark reader.
