One-sentence: Previous/next circular arrows for stepping between hymns in the lyrics reader — outlined circle for previous, filled brand circle for next.

```jsx
<PageNav onPrev={goPrev} onNext={goNext} prevDisabled={n === 1} />
```

Disabled arrows fade to 35% opacity.
