import * as React from 'react';
export interface NotchProps {
  className?: string;
  style?: React.CSSProperties;
  visible?: boolean;
}
export declare const Notch: React.FC<NotchProps>;
export default Notch;
