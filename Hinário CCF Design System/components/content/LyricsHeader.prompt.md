One-sentence: The reader sub-header combining a back chevron, the solid hymn-number badge, and the uppercase hymn title.

```jsx
<LyricsHeader number="001" title="Esta Paz" onBack={goBack} />
```

Pair it above `LyricsView` on the lyrics screen. `onBack` is optional.
