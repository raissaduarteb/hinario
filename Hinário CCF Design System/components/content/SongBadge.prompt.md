One-sentence: A small rounded chip that shows a hymn's reference number ("001", "L1", "H6") — solid orange for the current/primary hymn, muted orange for secondary results.

```jsx
<SongBadge>001</SongBadge>
<SongBadge tone="soft">H6</SongBadge>
```

Used at the left of `SongListItem` results and beside the title in the lyrics reader header.
